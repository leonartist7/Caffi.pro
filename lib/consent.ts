/**
 * The EXACT CASL consent sentence rendered next to the join-form checkbox.
 * One source of truth: the form renders it, /api/join stores it verbatim
 * as members.consent_text. Change it here and only here.
 */
export const CONSENT_TEXT = 'Yes — send me news and rewards from this café. Unsubscribe anytime.'
